<?php

if (!isset($_SERVER['argc']))
	die('this file can only be run from command line');

if ($argv[1] == 'digestday') require_once 'tools/cron_digestday.php';
if ($argv[1] == 'cleanmessagelog') require_once 'tools/cron_cleanmessagelog.php';
